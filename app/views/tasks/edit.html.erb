<h1>id:<%= @task.id %>の編集ページ</h1>

<%= form_with(model: @task, local:true ) do |f| %>
  
  <%= f.label :content, '編集してください' %>
  
  <%= f.text_field :content %>
  
  <%= f.submit '更新する' %>

<% end %>

<%= link_to 'タスク一覧へ', tasks_path %>