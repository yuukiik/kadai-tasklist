<h1>新規作成ページ</h1>

<%= form_with(model: @task, local: true) do |f| %>

  <%= f.label :content, '入力してください' %>

  <%= f.text_field :content %>

  <%= f.submit '投稿' %>

<% end %>

<%= link_to 'タスク一覧へ', tasks_path %>